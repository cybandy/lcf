export default defineAppConfig({
  ui: {
    colors: {
      primary: 'white-rock',
      neutral: 'slate'
    },
    input: {
      slots: {
        root: 'w-full'
      }
    },
    inputTags: {
      slots: {
        root: 'w-full'
      }
    },
    textarea: {
      slots: {
        root: 'w-full'
      }
    }
  }
})
